package com.exhanger.vvasiuk.repository;

import com.exhanger.vvasiuk.entity.FilesNumber;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FileNumberRepository extends JpaRepository<FilesNumber, Long> {
}
