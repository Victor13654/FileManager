

CREATE TABLE document (
  id INT AUTO_INCREMENT  PRIMARY KEY,
  documentName VARCHAR(250) NOT NULL
);

CREATE TABLE number(
     id INT AUTO_INCREMENT  PRIMARY KEY,
     home(10) int NOT NULL,
     dev(10) int NOT NULL,
     test(10) int NOT NULL,
     summ(10) int NOT NULL
);

